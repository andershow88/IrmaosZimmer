[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
# Sincroniza o schema no banco (Railway Postgres) e sobe o servidor na porta $PORT.
cmd = "npx prisma db push && npm run start"
